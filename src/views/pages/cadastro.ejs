<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html, body {
            height: 100vh;
        }
        
        body {
            display: flex;
            flex-direction: column;
        }
        
        header {
            background-color: lightgray;
            padding: 20px;
        }
        
        main {
            flex: 1;
            align-items: center;
            display: flex;
        }
        
        footer {
            background-color: lightgray;
            padding: 20px;
        }
        .login{
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .login > .col{
            padding: 20px;
            max-width: 60vw;
            margin: 10px;
            border: 2px solid lightgray;
            border-radius: 5%;
        }
    </style>
</head>
<body>

    <main>

        <div class="container">
            <div class="row login d-flex justify-content: center;
            align-items: center;">
                <div class="col shadow-lg ">
                    <h1>Cadastro de Usuário</h1>
                    <form action="/cadastro" method="post">
                        <div class="mb-3">
                            <label for="username" class="form-label">Nome de Usuário</label>
                            <input type="text" class="form-control" id="username" name="username" value="<%= nome %>" required>
                            <% if (mensagens.mensagemNome) { %>
                                <div><%= mensagens.mensagemNome %></div>
                            <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <input type="email" class="form-control" id="email" name="email" value="<%= email %>">
                            <% if (mensagens.mensagemEmail) { %>
                                <div><%= mensagens.mensagemEmail %></div>
                            <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="telefone" class="form-label">Telefone</label>
                            <input type="tel" class="form-control" id="telefone" name="telefone" value="<%= telefone %>" required>
                            <% if (mensagens.mensagemTelefone) { %>
                                <div><%= mensagens.mensagemTelefone %></div>
                            <% } %>
                        </div>
                        <div class="mb-3">
                            <label for="password" class="form-label">Senha</label>
                            <input type="password" class="form-control" id="password" name="password"  minlength="8" placeholder="********" required>
                        </div>
                        <div class="mb-3">
                            <label for="repeatpassword" class="form-label">Repetir Senha</label>
                            <input type="password" class="form-control" id="repeatpassword" name="repeatpassword" minlength="8" placeholder="********"  required>
                            <% if (mensagens.mensagemSenha) { %>
                                <div ><%= mensagens.mensagemSenha %></div>
                            <% } %>
                        </div>
                        <div class="row">
                            <div class="col-3">
                                <button type="submit" class="btn btn-primary">Cadastrar</button>
                            </div>
                            <div class="col-9">
                                <a href="/entrar">Já possui uma conta?</a>
                            </div>
                        </div>
                    </form>

                </div>
            </div>
        </div>

    </main>

</body>
</html>